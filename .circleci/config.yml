version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.8.6  # primary container for the build job
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
